# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project targets Expo SDK 54 specifically (not the latest SDK) so it installs
directly from the Play Store / App Store version of Expo Go via QR code, without
needing a development build or a USB-sideloaded client.
